= simple_form_for(@job, html: { class: 'form-horizontal'}) do |f|
	= f.input :title, lable: "Job title"
	= f.input :description, lable: "Job description"
	= f.input :company, lable: "Your company"
	= f.input :url, lable: "Link to job"
	%br/
	=f.button :submit
